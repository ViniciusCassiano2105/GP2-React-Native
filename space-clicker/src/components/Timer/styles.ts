import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container:{
       
        
    },
    textTimer:{
        color:"white",
        fontWeight:"bold",
        textAlign:"center",
        marginTop:30,
        fontSize:30
    }
})