import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    top:30,
    flex: 1, 
    justifyContent: "center", 
    alignItems: "center", 
  },
  textTimer: {
    color: "white",
    fontWeight: "bold",
    textAlign: "center",
    marginTop: 5, 
    fontSize: 30, 
    flexShrink: 1, 
  },
});
