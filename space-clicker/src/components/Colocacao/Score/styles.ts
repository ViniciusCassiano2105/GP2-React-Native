import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 8,
    backgroundColor: "#555",
    borderRadius: 8,
    marginVertical: 4,
  },
  text: {
    fontSize: 16,
    color: "#FFD700", 
    fontWeight: "bold",
  },
});
